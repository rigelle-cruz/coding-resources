// vowel count:

// turn string into an array
// look for vowels a, e, i, o, u
// create a new array
// count the number of vowels
// return the (count) of vowels

function getCount(str) {
return str.split("").find(vowel => {
  if(vowel === a)
  vowel.push
})
}
console.log(getCount("hello"))